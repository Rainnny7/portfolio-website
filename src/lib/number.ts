/**
 * Formats a number without trailing zeros.
 *
 * @param number the number to format
 * @returns the formatted number
 */
export const formatNumberWithCommas = (number: number) => number.toLocaleString();
